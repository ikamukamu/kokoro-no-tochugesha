# ISS-034 コーピングのタグ・分類の仕組み

|項目|内容|
|---|---|
|状態|検討中|
|優先度|中|
|担当|自分|

> このテンプレートは「最初から全部埋める」ことを目的としない。 ISSの進行に合わせて少しずつ育てる運用とする。

## 概要

現状、コーピングは単一のカテゴリ（5個）で分類されているが、これを複数タグを 持てるデータ構造に変更する。**カテゴリの「仕組み」を変えるのが本ISSの主目的で あり、タグの「中身」（具体的な分類名）の精査は範囲に含めない。**

## 背景

現状の5カテゴリ（ストレスから離れる／誰かに助けを求める／気持ちを整理する／ 認知を変化させる／気晴らしをする）は、NotebookLMに一覧を渡し、Geminiに 分割してもらったもので、実際の利用傾向から検証されたものではない。

チャピとの相談で、これらの分類軸が混在している可能性が指摘された。

- 誰かに助けを求める：行動・手段
- 認知を変化させる：作用
- 気晴らしをする：目的や効果
- ストレスから離れる：対処方針

将来的には、単一カテゴリではなく、以下のような複数軸のタグの方が 「必要なコーピングをすぐ見る」という目的に合う、という方向性が[[コーピング機能刷新構想]] に記載されている。

- 方法：身体を動かす、人と関わる、考える、休む
- 目的：落ち着く、離れる、整理する、元気を出す
- 状況：仕事中、家、外出中、寝る前
- 負荷：すぐできる、少し時間が必要

## 目的

- コーピングに複数タグを持てるデータ構造を用意し、将来的な分類軸の追加・ 見直しに耐えられるようにする
- 現状の単一カテゴリのデータを、タグ構造へ安全に移行する
- タグの「中身」（具体的な軸・タグ名）は、実際の登録・利用データが溜まって から見直すことを前提とし、本ISSでは仮のタグ構造で進める

## 要件

- コーピング1件につき、複数タグを持てるデータ構造にする
- 既存の5カテゴリのデータを、新しいタグ構造へ移行する（データ移行が必要）
- タグは初回登録時に必須にしない（[[ISS-032]]の「登録はできるだけ簡単に」 という設計原則に従う）
- プリセット側には、あらかじめタグを付けておけるようにする

## 設計方針

[[コーピング機能刷新構想]]の設計原則（後から育てられる設計にする）に沿う。

- データ構造は複数タグ対応（配列またはそれに準ずる形）にする
- UI上は、タグ選択を必須にせず、任意入力・後から追加できる形にする
- タグの分類軸（方法・目的・状況・負荷など）自体の確定は本ISSのスコープ外と し、まずは「複数タグを持てる」という構造のみを用意する
- 既存5カテゴリは、暫定的に「方法」または「目的」いずれかの軸へ仮マッピング して移行する（具体的なマッピング方法は実装時に検討）

## 判断理由

（データ移行方式・仮マッピング確定後に記載）

## 実装メモ

## 実装結果

- Commit：
- 実装内容：
- 変更ファイル：

## レビュー・フィードバック

## 懸念事項

- **データ移行を伴う**ため、既存の登録済みコーピング（プリセット・マイコー ピング双方）のカテゴリ情報を失わずに新構造へ移行する必要がある。移行スクリプト や移行前バックアップの検討が必要
- タグの分類軸（方法・目的・状況・負荷）を将来どう確定するかは未決。実装を 先に進めすぎると、軸の見直し時に再度データ移行が発生する可能性がある
- [[ISS-031]]の一覧表示・フィルタ機能とタグ構造が連動するため、[[ISS-031]] 側の実装状況と整合を取りながら進める

## 関連ISS

- [[ISS-031]]：一覧・フィルタ表示にタグが影響する
- [[ISS-033]]：プリセットのタグ引き継ぎと関連

## 関連ノート

## 関連設計

- [[コーピング機能刷新構想]]
- [[コーピング]]

## 完了条件

- コーピングが複数タグを持てるデータ構造になっている
- 既存の5カテゴリのデータが、タグ情報を失わずに新構造へ移行されている
- タグなしでも登録できる状態が維持されている

## 更新履歴

|日付|内容|
|---|---|
|2026-07-30|新規作成。[[コーピング機能刷新構想]]の分類軸の議論をもとに、データ構造変更・移行を主目的として概要・背景・目的・要件・設計方針を記載|