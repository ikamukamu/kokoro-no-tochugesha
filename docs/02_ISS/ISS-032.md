# ISS-032 コーピング登録UX改善（一次登録・後から編集・登録導線）

|項目|内容|
|---|---|
|状態|公開済|
|優先度|高|
|担当|自分|

> このテンプレートは「最初から全部埋める」ことを目的としない。 ISSの進行に合わせて少しずつ育てる運用とする。

## 概要

コーピングの登録を「タイトルだけで一次登録でき、詳細は後から追加できる」形に 改善する。あわせて、登録を促す導線（体験直後の呼びかけなど）を整備し、 「思いついた瞬間を逃さない」登録体験を作る。[[ISS-031]]の一覧・編集機能と セットで機能する。

## 背景

チャピとの相談で、以下の指摘があった。

- コーピングは「思いついた瞬間はあとでちゃんと書こうと思うと忘れる」ため、 一次登録はタイトルだけでもよい、あとから「どんな時？」「効果は？」「メモ」を 書けるようにする、というUXが望ましい
- 入力項目を最小限にする（名前だけでも保存できて、詳細は後から追加する）
- ホーム画面やコーピング実行直後に「これも登録する？」と促す（体験の直後は 一番ハードルが低い）

[[コーピング機能刷新構想]]では、この方針を「タイトル＋詳細の2階層」データ 構造として明文化している。

## 目的

- コーピングを「思いついた瞬間」に、負担なく登録できるようにする
- 登録のハードルを下げつつ、後から情報を育てられるようにする
- 「わたしのコーピング帳」に自然に書き足していく体験を作る

## 要件

- 新規登録時、タイトル（短い単語・フレーズ）のみで保存できる
- 登録後、[[ISS-031]]の一覧・編集機能から詳細（内容・メモ等）を追記できる
- コーピング実行直後など、体験のタイミングに合わせて登録を促す導線を用意する （例：処方箋実行後の「これも登録する？」）
- 登録フォームは最小限の入力項目にする

## 設計方針

[[コーピング機能刷新構想]]の「タイトル＋詳細の2階層」データ構造を前提とする。

- 一次登録：タイトルのみ必須。他はすべて任意
- 詳細情報（内容・メモ・登録日時など）は、一覧（[[ISS-031]]）からの編集で 後付けする
- 登録導線は、まず「コーピング実行直後」の1箇所から着手し、他の画面 （言葉サプリ、セルフトーク等）への展開は範囲を広げすぎず段階的に検討する （設計原則「登録はできるだけ簡単にする」に沿う）

## 判断理由

（設計確定後に記載）

## 実装メモ

## 実装結果

- Commit：`4a83fb7`（ISS-031実装時にタイトルのみ登録・後からの詳細追記を実現）、`fad0f03`（ISS-033実装時に、プリセット表示への常設ボタンとして登録導線を実現）
- 実装内容：独立したISSとしての実装は行わず、ISS-031（一覧・編集UI）とISS-033（プリセットからの追加ボタン）の実装によって、本ISSの3つの完了条件がすべて満たされた
- 変更ファイル：`index.html`（ISS-031・033に同じ）

## レビュー・フィードバック

- 「体験直後に呼びかける」という当初の想定（やってみた押下後に動的なプロンプトを出す）ではなく、「やってみた」ボタンの隣に常設ボタン（そのまま追加／自分用に変えて追加）を置く形で導線を実現した。動的な呼びかけは「くどくなる」との判断で見送り、常設ボタンで完了とした（2026-09-03）

## レビュー・フィードバック（v2・2026-09-07）

公開後、スマホ実機で新規追加・編集フォームを開くと、画面が大きくズームインされ、上部が見切れて使いづらいとの報告。

原因を確認したところ、`my-coping-title`・`my-coping-detail`・`my-coping-effect`の入力欄がすべて`text-xs`（12px相当）になっていた。iOS Safariには「フォーカスした入力欄のフォントサイズが16px未満だと自動的にズームインする」という挙動があり、フォームを開いた際に`my-coping-title`へ自動フォーカスする実装（`openMyCopingForm`内の`.focus()`）と組み合わさって、開いた瞬間に強制ズームインが発生していた。

## 要件（v2追加）：登録・編集フォームの自動ズーム防止

- `my-coping-title`・`my-coping-detail`・`my-coping-effect`の3つの入力欄のフォントサイズを、**16px以上**（Tailwindの`text-base`相当）に変更する
- 見た目上のバランスが崩れる場合は、ラベルや周農の余白も合わせて微調整してよい
- 同様の自動ズーム問題が、アプリ内の他の入力欄（`text-xs`や`text-sm`など16px未満のフォントサイズを使っているinput/textarea）でも起きていないか、あわせて確認する（本ISSでは発見した範囲のみ対応し、他に見つかった場合は別途起票する）

## 懸念事項

- 登録導線をアプリ内の複数箇所（言葉サプリ・セルフトーク・コラム法・駅長だより等） に広げる構想がチャピとの相談で出ているが、本ISSでは範囲を絞り、まずは コーピング実行直後の導線のみを対象とする。他画面への展開は将来ISSで検討
- [[ISS-031]]のデータ構造（一覧・編集機能）と一次登録のデータ構造が 食い違わないよう、実装順序・整合性に注意する

## 関連ISS

- [[ISS-031]]：本ISSの前提となる一覧・編集機能。依存関係あり
- [[ISS-033]]：プリセットのコピー登録と関連（登録導線という点で共通）

## 関連ノート

## 関連設計

- [[コーピング機能刷新構想]]
- [[世界観]]

## 完了条件

- [x] タイトルのみでコーピングを一次登録できる
- [x] 登録後、詳細情報を後から追記できる
- [x] コーピング実行直後に登録を促す導線が機能している

## 更新履歴

|日付|内容|
|---|---|
|2026-07-30|新規作成。チャピとの相談内容と[[コーピング機能刷新構想]]をもとに概要・背景・目的・要件・設計方針を記載|
|2026-09-03|ISS-033完了時点で、本ISSの3つの完了条件がすべて既存実装（ISS-031・033）によって満たされていることを確認。独立した追加実装は不要と判断し、状態を「公開済」に変更してクローズ|
|2026-09-07|公開後、スマホでの新規追加・編集フォームが自動ズームで見切れる不具合を発見。原因（入力欄フォントサイズ12px＋自動フォーカス）を特定し、16px以上への変更をv2要件として追加|