# AGENTS.md

このファイルはCodex（および他のAIエージェント）がこのリポジトリで作業する際の
基本ルールです。作業を始める前に必ず読んでください。

## このプロジェクトについて

「ココロの途中下車」— セルフケア支援のための静的シングルページWebアプリ。
診断や治療ではなく、あくまでセルフケアの補助を目的としています。

- index.html にHTML/CSS/JavaScriptがほぼすべて内包された単一ファイル構成
- ビルドプロセス・package.json・テスト構成なし
- Netlifyで公開

## 情報の正本（Single Source of Truth）

| 内容 | 正本の場所 |
|---|---|
| 設計・運営・PBL・開発ログ | OneNote |
| ソースコード | GitHub（main ブランチ） |
| Codexへの作業ルール | このファイル（AGENTS.md） |
| プロジェクト概要・技術構成 | README.md |

**重要：** 詳細な設計判断や仕様の背景はOneNote側にあります。このリポジトリ内の
ファイルだけでは設計意図が読み取れない箇所があるので、不明点は推測で実装せず、
ユーザーに確認してください。

## 開発環境

- 現在の正式な基準は GitHub の main ブランチ
- 開発環境はGitHubからCloneして構築する
- 作業ツリーがクリーンな状態（未追跡・未ステージ変更なし）から開始すること

## 実装上の既知事項

- **Firestore連携は休眠中**：コードは実装済みだが、`__firebase_config` 等の
  設定値を注入する仕組みがリポジトリ内に存在しないため、通常のNetlify配信では
  動作しません。現在の正式な保存先は **Local Storage** です。
  Firebase関連コードを「動いている前提」で拡張しないこと。
- **Gemini APIキーの扱いに注意**：`kokoro_gemini_api_key` はLocal Storageに
  平文保存され、JSONバックアップ機能にも含まれます。これはセキュリティ上の
  既知の課題です。バックアップ関連の修正時は特に注意してください。
- `header-apple` / `header-apple-ring` を参照する未使用コードが残存（実害なし）

## 作業ルール

1. **コード変更・Commit・Pushは、明示的な指示があった場合のみ行う。**
   調査・確認のみを依頼された場合は、コードを変更しない。
2. **勝手なリファクタリングは禁止。** 気になる点があれば提案として報告し、
   実行はユーザーの判断を待つ。
3. 仕様や設計意図が不明な場合は、憶測で実装せずユーザーに確認する。
   AIは既存コードから設計意図を推測しすぎないこと。「たぶんこういう設計だろう」
   という善意の補完は行わず、不明な場合はOneNoteの内容を確認するか、
   ユーザーに直接確認することを優先する。
4. Firebase/Firestoreを有効化する変更を行う場合は、以下を必ず設計し直すこと
   （現状のコードのままではデータ不整合が起きるため）：
   - Local Storageの既存データをFirestoreへ自動同期する処理
   - 同一IDでの競合解決ルールの見直し
   - Firestore書き込み・削除失敗時の利用者向け通知
5. Gitの操作前に `git status` で状態を確認し、意図しない変更が
   含まれていないか確認する。
6. 実装の途中で、指示された内容を超える設計判断（データ構造・保存方式・画面遷移・
   主要なUXの変更など）が必要になった場合は、そのまま実装を進めず、いったん
   立ち止まってユーザーに確認する。軽微な表示調整やバグ修正はこの限りではない。
7. 変更を実装したら、以下を簡潔に報告する。規模の小さい修正では一部省略してよい。
   - 変更したファイルと内容
   - 影響範囲
   - 懸念事項（あれば）

## 開発フロー

基本的な開発フローは以下の通りです。

```
設計（ChatGPT / OneNote）
    ↓
実装（Codex）
    ↓
動作確認
    ↓
Commit（ユーザー指示時）
    ↓
Push（ユーザー指示時）
```

これは基本フローであり、軽微な修正や調査のみの依頼では、ユーザーの指示により
一部の工程が省略されることがあります。フローを厳密に守ることよりも、
「コード変更・Commit・Pushはユーザーの明示的な指示があった場合のみ行う」
という原則を優先してください。

## 動作確認すべき主要機能

- ホーム
- コーピング処方
- CBTコラム法
- 呼吸（マインドフルネスタイマー）
- Firestore（現状は休眠中である点に留意）
- JSONバックアップの書き出し／読み込み
- スマホ表示
